{
  "name": "minimal-addon-example",
  "version": "1.0.0",
  "private": true,
  "description": "A Node-API addon in plain C, built with zig-native-build",
  "main": "./index.cjs",
  "scripts": {
    "build": "zig-native-build",
    "build:debug": "zig-native-build --debug",
    "test": "node --test"
  }
}

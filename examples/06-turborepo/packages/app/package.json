{
  "name": "@turbo-example/app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A JS package whose build step calls into the native addon next door",
  "main": "./index.mjs",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "node --test"
  },
  "dependencies": {
    "@turbo-example/checksum": "*"
  }
}

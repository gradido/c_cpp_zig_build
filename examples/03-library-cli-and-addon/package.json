{
  "name": "digest-example",
  "version": "1.0.0",
  "private": true,
  "description": "One core C library behind a Node addon, a static library and a CLI",
  "main": "./index.cjs",
  "scripts": {
    "build": "c-cpp-zig-build",
    "test": "node --test",
    "digest": "c-cpp-zig-build --step run --"
  }
}

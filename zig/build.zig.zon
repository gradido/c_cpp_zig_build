.{
    // Consumed as `@import("c_cpp_zig_build")` from a project's build.zig.
    .name = .c_cpp_zig_build,
    .version = "0.2.0",
    .fingerprint = 0xb623d410f2fa94b4,
    .minimum_zig_version = "0.15.1",

    // Deliberately empty. The template carries its own compile_commands
    // generator so that a project's dependency list stays its own.
    .dependencies = .{},

    .paths = .{
        "build.zig",
        "build.zig.zon",
        "src",
    },
}
